"""
testbench — Energy benchmarking suite for multi-agent consensus on GPU clusters.

Experiments:
    patterns   — 4-pattern consensus comparison (headline experiment)
    consensus  — NL consensus with multi-panel orchestration
    profile    — Per-hop energy profiling (serial + parallel)
    efficiency — KV cache x concurrency sweep
    experiment — Simple 2-node drafter/reviewer pipeline
"""

__version__ = "0.1.0"
